{1 2 add} call

# groups with one member are the same as just the member

# `->` should mean pop value and assign it to a name
# it takes a lazy argument, but it's not the deepest way to do that
# if `->` is user-definable then the whole language will be
